# 依赖注入模块指南

## 模块概述
- 提供声明式依赖注入核心能力
- 支持面向对象和函数式两种编程风格

## 使用指南
### 智能体交互规范
1. 通过`//ai:di`注释声明依赖关系
2. 使用结构体标签定义注入规则

## 代码结构
```
/internal
  /di-core      # 依赖注入核心实现
  /ast-parser   # 源码解析器
/pkg
  /di           # 对外暴露的API接口
```

## 测试规范
- 必须包含依赖关系解析测试用例
- 需验证循环依赖检测机制
- 性能基准测试要求：单次注入<5ms