// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * TestCaseService 测试用例管理服务（对应前端 packet-tab.vue 和 testcase-tab.vue 用例管理）
 * 
 * 时序图：
 * ┌─────────────────┐   LoadCaseList    ┌───────────────────┐
 * │ testcase-tab.vue│ ─────────────────> │ TestCaseService   │
 * └─────────────────┘                    │   (后端)          │
 * 
 * 	     │                                  └───────────────────┘
 * 	     │                                        │
 * 	     │                                        │ 遍历目录
 * 	     │                                        ▼
 * 	     │                                  cases/proto_cases/
 * 	     │                                        │
 * 	     │          返回用例列表                   │
 * 	     │ <──────────────────────────────────────┘
 * 	     │                                        │
 * 	     ▼                                        ▼
 * 	显示用例下拉框                          解析 .json 元信息
 * 
 * ┌─────────────────┐   LoadTestCase    ┌───────────────────┐
 * │ testcase-tab.vue│ ─────────────────> │ TestCaseService   │
 * └─────────────────┘                    │   (后端)          │
 * 
 * 	     │                                  └───────────────────┘
 * 	     │                                        │
 * 	     │                                        │ LoadRecordFile()
 * 	     │                                        ▼
 * 	     │                                  ┌──────────────────┐
 * 	     │                                  │RecordFileService │
 * 	     │                                  └──────────────────┘
 * 	     │                                        │
 * 	     │          返回录制数据                   │
 * 	     │ <──────────────────────────────────────┘
 * 	     │                                        │
 * 	     ▼                                        ▼
 * 	设置到组件状态                          加载录制文件
 * 
 * ┌─────────────────┐   SaveTestCase    ┌───────────────────┐
 * │ testcase-tab.vue│ ─────────────────> │ TestCaseService   │
 * └─────────────────┘                    │   (后端)          │
 * 
 * 	     │                                  └───────────────────┘
 * 	     │                                        │
 * 	     │                                        │ SaveRecordFile()
 * 	     │                                        ▼
 * 	     │                                  cases/proto_cases/<name>.json
 * 	     │                                        │
 * 	     │          保存成功                      │
 * 	     │ <──────────────────────────────────────┘
 * 	     │
 * 	     ▼
 * 	刷新用例列表
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise, Create as $Create } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as $models from "./models.js";

/**
 * AppendTestCase 向已存在用例追加 Req 消息。
 * 若用例不存在则创建新用例；存在则保留原用例元信息，将新 Req 追加到末尾。
 * 供 packet-tab.vue "保存到用例" 的追加场景使用，避免覆盖 testcase-tab.vue 已维护的用例数据。
 */
export function AppendTestCase(name: string, data: $models.RecordFileData | null): $CancellablePromise<void> {
    return $Call.ByID(1409733373, name, data);
}

/**
 * DeleteTestCase 删除指定测试用例
 */
export function DeleteTestCase(name: string): $CancellablePromise<void> {
    return $Call.ByID(495933814, name);
}

/**
 * LoadTestCase 加载指定测试用例（自动过滤 Ack/Ntf，补全 direction 供重放）
 */
export function LoadTestCase(name: string): $CancellablePromise<$models.RecordFileData | null> {
    return $Call.ByID(4029720613, name).then(($result: any) => {
        return $$createType1($result);
    });
}

/**
 * LoadTestCaseList 获取所有测试用例列表
 */
export function LoadTestCaseList(): $CancellablePromise<($models.ProtoCaseMeta | null)[]> {
    return $Call.ByID(1121560513).then(($result: any) => {
        return $$createType4($result);
    });
}

/**
 * SaveTestCase 保存测试用例（仅 Req，省略 direction/offset_ms；兼容 LoadRecordFile 和 StartReplay）
 * name: 用例名称
 * data: 录制文件数据（从中提取消息）
 * 
 * 注意：SaveTestCase 语义为覆盖写入，用于 testcase-tab.vue 的"新增模块"/"删除消息"/"保存顺序"等场景。
 * 从 packet-tab.vue 多选 Req "追加"到已有用例时，请使用 AppendTestCase，避免覆盖已有数据。
 */
export function SaveTestCase(name: string, data: $models.RecordFileData | null): $CancellablePromise<void> {
    return $Call.ByID(3697613920, name, data);
}

// Private type creation functions
const $$createType0 = $models.RecordFileData.createFrom;
const $$createType1 = $Create.Nullable($$createType0);
const $$createType2 = $models.ProtoCaseMeta.createFrom;
const $$createType3 = $Create.Nullable($$createType2);
const $$createType4 = $Create.Array($$createType3);
