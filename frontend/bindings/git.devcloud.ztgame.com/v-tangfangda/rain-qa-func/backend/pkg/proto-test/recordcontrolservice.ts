// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * RecordControlService 录制控制服务（对应前端 packet-tab.vue 录制按钮）
 * 
 * 时序图：
 * ┌─────────────────┐    StartRecord(filterMode) ┌────────────────────┐
 * │ packet-tab.vue  │ ───────────────────> │ RecordControlService │
 * │   (前端)        │                     │   (后端)             │
 * └─────────────────┘                     └────────────────────┘
 * 
 * 		     │                                        │
 * 		     │                                        │ Start()
 * 		     │                                        ▼
 * 		     │                                  ┌──────────┐
 * 		     │                                  │RecordWorker│
 * 		     │                                  │  + TCP代理 │
 * 		     │                                  │  + HTTP代理│
 * 		     │                                  └──────────┘
 * 		     │                                        │
 * 	  ......................................................
 * 	  .      │          filterMode:客户端修改报文        │    .
 * 	  .      │ <──────────────────────────────────────┤    .
 * 	  .      │                                        │    .
 * 	  .      │                                        │    .
 * 	  .      │          filterMode:客户端发送修改后的报文 │    .
 * 	  .      │ -─────────────────────────────────────>│    .
 * 	  ......................................................
 * 		     │                                        │
 * 		     │          Event.Emit('record:progress')  │
 * 		     │ <──────────────────────────────────────┤
 * 		     │                                        │
 * 		     ▼                                        ▼
 * 		表格实时追加消息                          监听端口并录制
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise, Create as $Create } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as cases$0 from "./cases/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as protocol$0 from "./msg/models.js";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as $models from "./models.js";

/**
 * GetRecordStatus 获取当前录制状态
 */
export function GetRecordStatus(): $CancellablePromise<$models.RecordProgress | null> {
    return $Call.ByID(1161007190).then(($result: any) => {
        return $$createType1($result);
    });
}

/**
 * GetRecording 获取当前内存中的录制数据（供"保存为用例"使用）
 */
export function GetRecording(): $CancellablePromise<protocol$0.Recording | null> {
    return $Call.ByID(1010580044).then(($result: any) => {
        return $$createType3($result);
    });
}

/**
 * ReleaseAllPending 放行所有连接的 pending 消息
 */
export function ReleaseAllPending(editsJSON: string): $CancellablePromise<void> {
    return $Call.ByID(521219514, editsJSON);
}

/**
 * ReleasePendingMessages 放行指定连接的 pending 消息（代理内写入 serverConn）
 */
export function ReleasePendingMessages(connID: number, editsJSON: string): $CancellablePromise<void> {
    return $Call.ByID(2099128251, connID, editsJSON);
}

/**
 * SetFilterMode 关闭/开启实时拦截（关闭后后续消息透传）
 */
export function SetFilterMode(enabled: boolean): $CancellablePromise<void> {
    return $Call.ByID(373762580, enabled);
}

/**
 * Start 启动监听并立即开始录制（兼容旧接口）
 * 新代码建议分别调用 StartListen + StartRecord
 */
export function Start(serverAddr: string, httpAddr: string, filterMode: boolean): $CancellablePromise<void> {
    return $Call.ByID(254314537, serverAddr, httpAddr, filterMode);
}

/**
 * StartListen 启动本地 TCP/HTTP 监听（应用启动时调用）
 * serverAddr: 目标 TCP 服务器地址（如 "10.254.114.204:18000"）
 * httpAddr: 目标 HTTP 地址（如 "10.254.114.204:20144"）
 * tcpListenPort: 本地 TCP 监听端口
 * httpListenPort: 本地 HTTP 监听端口
 */
export function StartListen(serverAddr: string, httpAddr: string, tcpListenPort: number, httpListenPort: number): $CancellablePromise<void> {
    return $Call.ByID(3175119962, serverAddr, httpAddr, tcpListenPort, httpListenPort);
}

/**
 * StartRecord 开始录制（监听已启动后调用）
 */
export function StartRecord(filterMode: boolean): $CancellablePromise<void> {
    return $Call.ByID(1117455256, filterMode);
}

/**
 * Stop 停止录制和监听（完整停止，兼容旧逻辑）
 */
export function Stop(): $CancellablePromise<void> {
    return $Call.ByID(1304442435);
}

/**
 * StopListen 停止监听（关闭 TCP/HTTP 监听器）
 */
export function StopListen(): $CancellablePromise<void> {
    return $Call.ByID(1244946540);
}

/**
 * StopRecord 停止录制（保留监听）
 */
export function StopRecord(): $CancellablePromise<void> {
    return $Call.ByID(2425014770);
}

// Private type creation functions
const $$createType0 = $models.RecordProgress.createFrom;
const $$createType1 = $Create.Nullable($$createType0);
const $$createType2 = cases$0.Recording.createFrom;
const $$createType3 = $Create.Nullable($$createType2);
